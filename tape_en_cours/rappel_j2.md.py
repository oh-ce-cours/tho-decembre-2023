# Rappel (2eme jour après-midi)

Manipulations et mise en pratique de boucles :
    * `for`quand on connait le nombre de pas que l'on va faire
    * `while`quand on ne le connait pas
    
Manipulation de conditions avec if
    * if CONDITION:
    * (elif CONDITION:) (optionnel, autant qu'on veut, permet de gérer les cas sinon si)
    * (else:) (optionnel, on peut en mettre qu'un seul)
        * on permet de capturer tous les autres cas
    
    
    

ma_liste = []
    if ma_liste:
<=> if len(ma_liste) > 0:
    